name: Format M3 alert state

on:
  push:
    branches:
      - feature/m3-alert-state
    paths:
      - .github/workflows/format-m3-alert-state.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/m3-alert-state
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run format
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - name: Commit formatting and remove workflow
        run: |
          rm .github/workflows/format-m3-alert-state.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add apps/worker/src/monitor-alert-state.ts apps/worker/src/monitor-alert-state.test.ts apps/worker/src/monitor-reconcile.ts apps/worker/package.json docs/MONITORING.md .github/workflows/format-m3-alert-state.yml
          git commit -m "style(worker): format alert lifecycle state"
          git push
